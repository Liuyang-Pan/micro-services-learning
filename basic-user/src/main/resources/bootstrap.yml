#bootstrap.xml服务启动是本配置会最先加载
server:
  port: 8080
spring:
  application:
    name: basic-user
  profiles:
    active: dev #开发环境配置
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        file-extension: yaml #指定文件后缀名